const CONFIG: { rewards: number; maxTimestamp: number; lambda: number; minPremiums: number } = {
  rewards: 3000000,
  maxTimestamp: 1637875800,
  lambda: 0.6,
  minPremiums: 50,
}

export default CONFIG
